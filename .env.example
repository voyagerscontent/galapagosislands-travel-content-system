# Optional: if you prefer secrets in .env instead of hard-coded in config/SITE_CONFIG.md.
# The SITE_CONFIG BRAND BLOCK automation: keys take precedence if filled.
AIRTABLE_PAT=
AIRTABLE_BASE=app
AIRTABLE_TABLE=tbl
N8N_BASE_URL=https://
N8N_INTAKE_WEBHOOK=https://
N8N_API_KEY=
